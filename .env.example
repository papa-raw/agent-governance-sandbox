# LLM agent delegates (either key works, OpenRouter preferred)
# If not set, the simulation uses deterministic agent behavior
VITE_OPENROUTER_API_KEY=
VITE_ANTHROPIC_API_KEY=

# Set false to disable Lit Protocol threshold encryption
VITE_LIT_ENABLED=true
